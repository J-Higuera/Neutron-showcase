@font-face {
  font-family: "Fraunces";
  src: url("assets/fonts/fraunces-500.ttf") format("truetype");
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Fraunces";
  src: url("assets/fonts/fraunces-600.ttf") format("truetype");
  font-weight: 600;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Fraunces";
  src: url("assets/fonts/fraunces-700.ttf") format("truetype");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Fraunces";
  src: url("assets/fonts/fraunces-800.ttf") format("truetype");
  font-weight: 800;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "IBM Plex Mono";
  src: url("assets/fonts/ibm-plex-mono-400.ttf") format("truetype");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "IBM Plex Mono";
  src: url("assets/fonts/ibm-plex-mono-500.ttf") format("truetype");
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "IBM Plex Mono";
  src: url("assets/fonts/ibm-plex-mono-600.ttf") format("truetype");
  font-weight: 600;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Manrope";
  src: url("assets/fonts/manrope-400.ttf") format("truetype");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Manrope";
  src: url("assets/fonts/manrope-500.ttf") format("truetype");
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Manrope";
  src: url("assets/fonts/manrope-600.ttf") format("truetype");
  font-weight: 600;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Manrope";
  src: url("assets/fonts/manrope-700.ttf") format("truetype");
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: "Manrope";
  src: url("assets/fonts/manrope-800.ttf") format("truetype");
  font-weight: 800;
  font-style: normal;
  font-display: swap;
}

:root {
  color-scheme: light;
  --espresso: #251813;
  --charcoal: #151513;
  --charcoal-2: #20201c;
  --crema: #f0dfbf;
  --parchment: #f7efd9;
  --paper: #fff8e9;
  --copper: #c8753d;
  --heat: #df9b59;
  --cherry: #a73931;
  --green: #49684e;
  --green-bright: #8fa277;
  --ink: #271d17;
  --muted: #756556;
  --label-dark: #3b2a21;
  --curve-dark: #344c39;
  --curve-red: #7f231d;
  --line: rgba(39, 29, 23, .16);
  --light-line: rgba(247, 239, 217, .22);
  --shadow: 0 24px 70px rgba(26, 15, 9, .22);
  --ease: cubic-bezier(.16, 1, .3, 1);
  --sans: "Manrope", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --serif: "Fraunces", Georgia, serif;
  --mono: "IBM Plex Mono", "SFMono-Regular", Consolas, monospace;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  overflow-x: hidden;
}

body {
  margin: 0;
  background: var(--parchment);
  color: var(--ink);
  font-family: var(--sans);
  font-size: 16px;
  line-height: 1.6;
  overflow-x: hidden;
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  z-index: -1;
  pointer-events: none;
  opacity: .28;
  background:
    radial-gradient(circle at 14% 22%, rgba(167, 57, 49, .16), transparent 22rem),
    radial-gradient(circle at 88% 18%, rgba(73, 104, 78, .18), transparent 24rem),
    linear-gradient(90deg, rgba(37, 24, 19, .035) 1px, transparent 1px),
    linear-gradient(rgba(37, 24, 19, .03) 1px, transparent 1px);
  background-size: auto, auto, 42px 42px, 42px 42px;
}

img,
svg {
  display: block;
  max-width: 100%;
}

img {
  height: auto;
}

a {
  color: inherit;
  text-decoration: none;
}

a:focus-visible,
button:focus-visible {
  outline: 3px solid var(--heat);
  outline-offset: 4px;
}

.skip-link {
  position: fixed;
  left: 1rem;
  top: .75rem;
  z-index: 20;
  padding: .7rem 1rem;
  border-radius: 999px;
  background: var(--paper);
  color: var(--espresso);
  transform: translateY(-140%);
  transition: transform .2s var(--ease);
}

.skip-link:focus {
  transform: translateY(0);
}

.site-header {
  position: absolute;
  inset: 0 0 auto;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  width: min(1180px, calc(100% - 2rem));
  margin: 0 auto;
  padding: 1.1rem 0;
  color: var(--crema);
}

.brand {
  display: inline-flex;
  align-items: center;
  min-height: 44px;
}

.brand__word,
.wordmark {
  position: relative;
  display: inline-block;
  font-family: var(--serif);
  font-weight: 780;
  font-variation-settings: "WONK" 1, "SOFT" 50;
  line-height: .9;
}

.brand__word {
  font-size: clamp(1.7rem, 1.28rem + 1.4vi, 2.35rem);
}

.brand__o,
.accent-o {
  position: relative;
}

.brand__o::after,
.accent-o::after {
  content: "";
  position: absolute;
  left: 50%;
  top: -.18em;
  width: .56em;
  aspect-ratio: 1;
  border: .08em solid var(--heat);
  border-left-color: var(--green-bright);
  border-radius: 50%;
  transform: translateX(-50%) rotate(-22deg);
  box-shadow: 0 0 0 .08em rgba(240, 223, 191, .08);
}

.nav-links,
.site-footer nav {
  display: flex;
  flex-wrap: wrap;
  gap: .35rem;
  justify-content: flex-end;
  font-family: var(--mono);
  font-size: .78rem;
  text-transform: uppercase;
}

.nav-links a,
.site-footer nav a {
  min-height: 36px;
  display: inline-flex;
  align-items: center;
  padding: .4rem .7rem;
  border: 1px solid rgba(240, 223, 191, .2);
  border-radius: 999px;
  background: rgba(21, 21, 19, .24);
}

.section-dark {
  background: var(--espresso);
  color: var(--crema);
}

.section-light {
  background: linear-gradient(180deg, var(--parchment), #ead8b4);
}

.section-warm {
  background:
    radial-gradient(circle at 15% 0%, rgba(200, 117, 61, .22), transparent 22rem),
    linear-gradient(135deg, #e9d2a5, #f6edda 48%, #d5b27c);
}

.hero {
  position: relative;
  isolation: isolate;
  min-height: 100svh;
  padding: clamp(5.6rem, 8vi, 8rem) 0 clamp(3rem, 6vi, 5rem);
  overflow: hidden;
}

.hero__terrain {
  position: absolute;
  inset: 0;
  z-index: -2;
  opacity: .3;
}

.hero__terrain::after {
  content: "";
  position: absolute;
  inset: 0;
  background:
    linear-gradient(90deg, rgba(37, 24, 19, .96), rgba(37, 24, 19, .84) 43%, rgba(37, 24, 19, .5)),
    linear-gradient(180deg, rgba(21, 21, 19, .4), var(--espresso));
}

.hero__terrain img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hero__inner,
.origins,
.roastery,
.cupping,
.club,
.wholesale__inner,
.site-footer {
  width: min(1180px, calc(100% - 2rem));
  margin-inline: auto;
}

.hero__inner {
  display: grid;
  grid-template-columns: minmax(0, .92fr) minmax(20rem, 1.08fr);
  align-items: center;
  gap: clamp(2rem, 4vi, 4.5rem);
}

.hero__copy {
  min-width: 0;
  overflow-wrap: break-word;
}

.proof-row,
.mode-chips,
.lot-card__meta,
.contact-lines {
  display: flex;
  flex-wrap: wrap;
  gap: .5rem;
}

.proof-row span,
.mode-chips span,
.lot-card__meta span,
.section-kicker,
.tier-card__tag {
  font-family: var(--mono);
  font-size: .74rem;
  font-weight: 600;
  text-transform: uppercase;
}

.proof-row span,
.mode-chips span {
  border: 1px solid rgba(240, 223, 191, .2);
  border-radius: 999px;
  padding: .45rem .7rem;
  background: rgba(247, 239, 217, .08);
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1,
h2,
h3 {
  font-family: var(--serif);
  line-height: .95;
  text-wrap: balance;
}

h1 {
  margin: 1.4rem 0 1.2rem;
  font-size: clamp(3rem, 2.08rem + 4vi, 6.3rem);
  font-weight: 760;
}

h1 .wordmark {
  display: block;
  color: var(--heat);
  margin-bottom: .35rem;
  font-size: .86em;
}

.hero-line {
  display: block;
}

h2 {
  font-size: clamp(2.1rem, 1.55rem + 2.2vi, 4.45rem);
  font-weight: 740;
}

h3 {
  font-size: clamp(1.6rem, 1.35rem + .8vi, 2.2rem);
}

p {
  text-wrap: pretty;
}

.hero__lede {
  max-width: 44rem;
  color: rgba(240, 223, 191, .82);
  font-size: clamp(1.04rem, .98rem + .35vi, 1.25rem);
}

.cta-row {
  display: flex;
  flex-wrap: wrap;
  gap: .75rem;
  margin: 1.7rem 0 1rem;
}

.cta-row--center {
  justify-content: center;
}

.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 48px;
  padding: .85rem 1.05rem;
  border-radius: 999px;
  border: 1px solid transparent;
  font-weight: 800;
  line-height: 1.1;
  transition: transform .18s var(--ease), background .18s var(--ease), border-color .18s var(--ease);
}

.button--primary {
  background: var(--cherry);
  color: #fff6e6;
  box-shadow: 0 16px 36px rgba(68, 17, 13, .25);
}

.button--secondary {
  background: rgba(240, 223, 191, .08);
  color: currentColor;
  border-color: rgba(240, 223, 191, .28);
}

.section-light .button--secondary,
.section-warm .button--secondary {
  border-color: rgba(39, 29, 23, .24);
  background: rgba(255, 248, 233, .62);
}

.vortex-shell {
  min-width: 0;
}

.vortex-stage {
  position: relative;
  width: min(100%, 620px);
  aspect-ratio: 1;
  margin-inline: auto;
  border: 1px solid rgba(240, 223, 191, .2);
  border-radius: 50%;
  background:
    radial-gradient(circle at 50% 50%, rgba(200, 117, 61, .24), transparent 33%),
    radial-gradient(circle at 50% 50%, rgba(18, 41, 31, .7), rgba(21, 21, 19, .24) 68%);
  box-shadow: inset 0 0 0 1px rgba(255, 248, 233, .06), var(--shadow);
  overflow: hidden;
}

.vortex-stage::before {
  content: "";
  position: absolute;
  inset: 8.5%;
  border: 1px dashed rgba(240, 223, 191, .18);
  border-radius: 50%;
}

.vortex-svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
}

.orbit {
  stroke: rgba(240, 223, 191, .18);
  stroke-width: 1.2;
  stroke-dasharray: 6 13;
}

.roast-curve {
  stroke: url(#curveGradient);
  stroke-width: 8;
  stroke-linecap: round;
  filter: drop-shadow(0 0 18px rgba(208, 122, 63, .32));
  stroke-dasharray: 1420;
  stroke-dashoffset: 1420;
}

.data-ticks path {
  stroke: rgba(247, 239, 217, .46);
  stroke-width: 2;
  stroke-linecap: round;
}

.bean {
  offset-path: path("M120 456 C 198 296, 380 546, 470 354 C 551 181, 298 148, 271 289 C 248 411, 430 411, 414 299 C 402 217, 296 218, 309 300 C 319 362, 395 342, 377 291");
  offset-rotate: 0deg;
}

.bean--green {
  fill: var(--green-bright);
  offset-distance: 8%;
}

.bean--copper {
  fill: var(--heat);
  offset-distance: 48%;
}

.bean--red {
  fill: var(--cherry);
  offset-distance: 78%;
}

.vortex-label {
  fill: rgba(240, 223, 191, .72);
  font-family: var(--mono);
  font-size: 14px;
  letter-spacing: 0;
}

.orbit-card {
  position: absolute;
  z-index: 2;
  width: clamp(8.8rem, 25%, 10.7rem);
  min-height: 92px;
  padding: .85rem;
  border: 1px solid rgba(240, 223, 191, .24);
  border-radius: 8px;
  background: rgba(32, 32, 28, .78);
  color: var(--crema);
  box-shadow: 0 16px 44px rgba(0, 0, 0, .2);
  backdrop-filter: blur(10px);
  transition: transform .18s var(--ease), border-color .18s var(--ease);
}

.orbit-card span {
  display: block;
  margin-bottom: .35rem;
  color: var(--heat);
  font-family: var(--serif);
  font-size: 1.45rem;
  font-weight: 740;
  line-height: .95;
}

.orbit-card strong {
  display: block;
  font-family: var(--mono);
  font-size: .68rem;
  font-weight: 500;
  line-height: 1.45;
  text-transform: uppercase;
}

.orbit-card--farm { left: 8%; top: 16%; }
.orbit-card--roast { right: 8%; top: 18%; }
.orbit-card--cup { right: 10%; bottom: 13%; }
.orbit-card--club { left: 10%; bottom: 15%; }

.artifact {
  position: absolute;
  z-index: 1;
  overflow: hidden;
  border: 1px solid rgba(240, 223, 191, .22);
  border-radius: 8px;
  box-shadow: 0 18px 46px rgba(0, 0, 0, .26);
}

.artifact img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.artifact span {
  position: absolute;
  left: .7rem;
  bottom: .65rem;
  padding: .28rem .45rem;
  border-radius: 999px;
  background: rgba(21, 21, 19, .66);
  color: var(--crema);
  font-family: var(--mono);
  font-size: .64rem;
  text-transform: uppercase;
}

.artifact--cherries {
  left: 34%;
  top: 7%;
  width: 31%;
  aspect-ratio: 5 / 3;
  transform: rotate(-4deg);
}

.artifact--green {
  right: 32%;
  bottom: 7%;
  width: 24%;
  aspect-ratio: 4 / 5;
  transform: rotate(5deg);
}

.origins,
.roastery,
.cupping,
.club,
.wholesale {
  padding-block: clamp(4.5rem, 8vi, 7rem);
}

.section-kicker {
  margin-bottom: .75rem;
  color: var(--curve-red);
  letter-spacing: .08em;
}

.section-dark .section-kicker {
  color: var(--heat);
}

.section-heading {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(16rem, .75fr);
  gap: clamp(1rem, 4vi, 4rem);
  align-items: end;
  margin-bottom: clamp(2rem, 5vi, 4rem);
}

.section-heading p {
  color: var(--muted);
}

.section-dark .section-heading p {
  color: rgba(240, 223, 191, .72);
}

.origins-layout {
  display: grid;
  grid-template-columns: minmax(15rem, .62fr) minmax(0, 1.38fr);
  gap: clamp(1.2rem, 3vi, 2rem);
  align-items: start;
}

.evidence-panel,
.process-photo,
.packing-panel,
.cupping__panel {
  position: relative;
  overflow: hidden;
  border-radius: 8px;
  box-shadow: var(--shadow);
}

.evidence-panel {
  min-height: 560px;
  transform: rotate(-1.2deg);
  background: var(--charcoal);
}

.evidence-panel img,
.process-photo img,
.packing-panel img,
.cupping__panel > img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.evidence-panel img {
  position: absolute;
  inset: 0;
}

.evidence-panel::after,
.process-photo::after,
.packing-panel::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, transparent 42%, rgba(21, 21, 19, .78));
}

.evidence-panel div,
.process-photo figcaption,
.packing-panel figcaption {
  position: absolute;
  inset: auto 1rem 1rem;
  z-index: 1;
  color: var(--crema);
}

.evidence-panel span,
.process-photo figcaption,
.packing-panel figcaption {
  font-family: var(--mono);
  font-size: .76rem;
  text-transform: uppercase;
}

.evidence-panel p {
  margin: .4rem 0 0;
  max-width: 23rem;
}

.lot-grid,
.tier-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
}

.lot-card,
.tier-card,
.ledger {
  border: 1px solid var(--line);
  border-radius: 8px;
  background: rgba(255, 248, 233, .78);
  box-shadow: 0 18px 44px rgba(82, 52, 26, .1);
}

.lot-card {
  position: relative;
  overflow: hidden;
  min-height: 430px;
  padding: 1.1rem;
}

.lot-card::before {
  content: "";
  position: absolute;
  inset: auto -14% -18% auto;
  width: 13rem;
  aspect-ratio: 1;
  border: 1px solid rgba(52, 76, 57, .38);
  border-radius: 50%;
  background: radial-gradient(circle, rgba(139, 162, 118, .2), transparent 62%);
  transition: transform .22s var(--ease), opacity .22s var(--ease);
}

.lot-card__top {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 1.25rem;
}

.lot-card__top span,
dt,
.lot-card em {
  font-family: var(--mono);
  font-size: .72rem;
  text-transform: uppercase;
  color: var(--label-dark);
  font-weight: 700;
  font-style: normal;
}

.lot-card__top strong {
  font-family: var(--serif);
  font-size: 1.55rem;
  line-height: .95;
  text-align: right;
}

dl {
  display: grid;
  gap: .65rem;
  margin: 0 0 1rem;
}

dt,
dd {
  margin: 0;
}

dd {
  font-weight: 800;
}

.lot-card__meta span {
  padding: .36rem .55rem;
  border: 1px solid rgba(39, 29, 23, .26);
  border-radius: 999px;
  background: rgba(255, 255, 255, .38);
  color: var(--label-dark);
}

.mini-curve {
  width: 100%;
  height: 72px;
  margin-top: 1rem;
}

.mini-curve path {
  fill: none;
  stroke: var(--curve-dark);
  stroke-width: 4;
  stroke-linecap: round;
}

.mini-curve circle {
  fill: var(--curve-red);
}

.roastery__grid {
  display: grid;
  grid-template-columns: minmax(17rem, .72fr) minmax(0, 1.28fr);
  gap: clamp(1.2rem, 4vi, 3.5rem);
  align-items: center;
  width: min(1180px, calc(100% - 2rem));
  margin-inline: auto;
}

.process-photo {
  min-height: 680px;
  background: var(--charcoal);
}

.ledger {
  padding: clamp(1.2rem, 3vi, 2rem);
}

.ledger p {
  color: #4e3a2c;
}

.profile-curve {
  margin: 1.4rem 0 1.2rem;
  overflow: visible;
}

.profile-curve__base {
  fill: none;
  stroke: var(--curve-red);
  stroke-width: 5;
  stroke-linecap: round;
  stroke-dasharray: 820;
  stroke-dashoffset: 820;
}

.profile-points circle {
  fill: var(--paper);
  stroke: var(--curve-dark);
  stroke-width: 3;
}

.profile-points text {
  fill: var(--ink);
  font-family: var(--mono);
  font-size: 13px;
}

.ledger-steps {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: .85rem;
  padding: 0;
  margin: 0;
  list-style: none;
}

.ledger-steps li {
  padding: .9rem;
  border: 1px solid rgba(39, 29, 23, .14);
  border-radius: 8px;
  background: rgba(255, 248, 233, .54);
}

.ledger-steps span {
  display: block;
  color: var(--curve-red);
  font-family: var(--mono);
  font-size: .72rem;
  font-weight: 700;
}

.ledger-steps strong {
  display: block;
  margin: .2rem 0;
}

.ledger-steps p {
  margin: 0;
  font-size: .93rem;
}

.cupping__panel {
  display: grid;
  grid-template-columns: minmax(15rem, .78fr) minmax(0, 1fr);
  min-height: 520px;
  background: var(--charcoal);
}

.score-sheet {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: clamp(1.2rem, 5vi, 4rem);
  background:
    linear-gradient(90deg, rgba(247, 239, 217, .94), rgba(247, 239, 217, .82)),
    linear-gradient(0deg, rgba(37, 24, 19, .08) 1px, transparent 1px);
  background-size: auto, 100% 34px;
}

.sensory-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(min(120px, 100%), 1fr));
  gap: .7rem;
  margin-top: 1rem;
}

.sensory-grid span {
  min-height: 86px;
  display: grid;
  place-items: center;
  border: 1px solid rgba(39, 29, 23, .16);
  border-radius: 8px;
  background: rgba(255, 248, 233, .66);
  color: var(--green);
  font-family: var(--mono);
  font-weight: 700;
  text-transform: uppercase;
}

.club {
  position: relative;
  isolation: isolate;
  overflow: hidden;
}

.club::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  background:
    radial-gradient(circle at 12% 35%, rgba(167, 57, 49, .25), transparent 28rem),
    radial-gradient(circle at 85% 16%, rgba(73, 104, 78, .34), transparent 22rem);
}

.club-flow {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: .7rem;
  margin-bottom: 1rem;
}

.club-flow span {
  min-height: 74px;
  display: flex;
  align-items: center;
  padding: .8rem;
  border: 1px solid var(--light-line);
  border-radius: 8px;
  background: rgba(247, 239, 217, .08);
  font-family: var(--mono);
  font-size: .78rem;
  text-transform: uppercase;
}

.tier-grid {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.tier-card {
  display: flex;
  flex-direction: column;
  min-height: 390px;
  padding: 1.15rem;
  background: rgba(247, 239, 217, .1);
  border-color: var(--light-line);
  color: var(--crema);
}

.tier-card p {
  color: rgba(240, 223, 191, .74);
}

.tier-card .button {
  margin-top: auto;
}

.tier-card__tag {
  color: var(--heat);
}

.dial {
  display: grid;
  gap: .5rem;
  margin: 1rem 0 1.2rem;
}

.dial span {
  position: relative;
  display: flex;
  justify-content: space-between;
  gap: .75rem;
  color: rgba(240, 223, 191, .76);
  font-family: var(--mono);
  font-size: .72rem;
  text-transform: uppercase;
}

.dial span::after {
  content: "";
  align-self: center;
  width: 46%;
  height: 6px;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--heat) var(--v), rgba(240, 223, 191, .16) 0);
}

.packing-panel {
  aspect-ratio: 16 / 6;
  margin: 1rem 0 0;
  background: var(--charcoal);
}

.wholesale__inner {
  display: grid;
  grid-template-columns: minmax(0, 1.15fr) minmax(16rem, .85fr);
  gap: clamp(1rem, 4vi, 4rem);
  align-items: start;
}

.wholesale__copy {
  padding-top: .35rem;
}

.contact-lines {
  margin: 1rem 0;
  font-family: var(--mono);
  font-size: .86rem;
}

.contact-lines > * {
  padding: .35rem .55rem;
  border: 1px solid rgba(39, 29, 23, .16);
  border-radius: 999px;
  background: rgba(255, 248, 233, .46);
}

.site-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  padding-block: 1.3rem;
  color: var(--crema);
  background: var(--espresso);
}

body:has(.site-footer) {
  background: var(--espresso);
}

.site-footer p {
  margin: 0;
  color: rgba(240, 223, 191, .62);
  font-size: .85rem;
}

@media (hover: hover) and (pointer: fine) {
  .button:hover,
  .orbit-card:hover,
  .lot-card:hover,
  .tier-card:hover {
    transform: translateY(-4px);
  }

  .lot-card:hover::before {
    transform: scale(1.15) translate(-6%, -5%);
    opacity: .9;
  }

  .lot-card:hover .mini-curve path {
    stroke: var(--cherry);
  }

  .tier-card:hover .dial span::after {
    background: linear-gradient(90deg, var(--green-bright) var(--v), rgba(240, 223, 191, .18) 0);
  }
}

@media (prefers-reduced-motion: no-preference) {
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity .7s var(--ease), transform .7s var(--ease);
    transition-delay: calc(var(--i, 0) * 70ms);
  }

  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .vortex-stage.is-visible .roast-curve,
  .vortex-shell.is-visible .roast-curve {
    animation: drawCurve 1.2s var(--ease) .2s forwards;
  }

  .ledger.is-visible .profile-curve__base {
    animation: drawProfile 1.1s var(--ease) .1s forwards;
  }

  .vortex-stage.is-visible .bean--green,
  .vortex-shell.is-visible .bean--green {
    animation: beanTravel 7s var(--ease) .45s infinite alternate;
  }

  .vortex-stage.is-visible .bean--copper,
  .vortex-shell.is-visible .bean--copper {
    animation: beanTravelMid 7.4s var(--ease) .65s infinite alternate;
  }

  .vortex-stage.is-visible .bean--red,
  .vortex-shell.is-visible .bean--red {
    animation: beanTravelLate 7.8s var(--ease) .8s infinite alternate;
  }
}

@keyframes drawCurve {
  to { stroke-dashoffset: 0; }
}

@keyframes drawProfile {
  to { stroke-dashoffset: 0; }
}

@keyframes beanTravel {
  to { offset-distance: 32%; }
}

@keyframes beanTravelMid {
  from { offset-distance: 44%; }
  to { offset-distance: 68%; }
}

@keyframes beanTravelLate {
  from { offset-distance: 70%; }
  to { offset-distance: 94%; }
}

@media (max-width: 68rem) {
  .site-header {
    position: relative;
    background: var(--espresso);
    width: 100%;
    padding: .75rem 1rem;
  }

  .hero {
    min-height: auto;
    padding-top: 2rem;
  }

  .hero__inner,
  .origins-layout,
  .roastery__grid,
  .section-heading,
  .wholesale__inner {
    grid-template-columns: 1fr;
  }

  .vortex-stage {
    width: min(100%, 560px);
  }

  .evidence-panel {
    min-height: 420px;
  }

  .process-photo {
    min-height: 520px;
  }

  .club-flow,
  .tier-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 46rem) {
  .site-header,
  .site-footer {
    flex-direction: column;
    align-items: flex-start;
  }

  .nav-links,
  .site-footer nav {
    justify-content: flex-start;
  }

  .hero__inner,
  .origins,
  .roastery,
  .cupping,
  .club,
  .wholesale__inner,
  .site-footer {
    width: min(100% - 1rem, 38rem);
  }

  .hero {
    padding-top: 1.5rem;
  }

  h1 {
    font-size: clamp(2.15rem, 1.9rem + 1.35vi, 2.85rem);
  }

  h1 > span:not(.wordmark) {
    overflow-wrap: break-word;
  }

  .cta-row {
    align-items: stretch;
  }

  .button {
    width: 100%;
  }

  .vortex-stage {
    aspect-ratio: auto;
    width: 100%;
    border-radius: 8px;
    padding: 1rem;
    display: grid;
    gap: .75rem;
    background: linear-gradient(180deg, rgba(18, 41, 31, .65), rgba(32, 32, 28, .74));
  }

  .vortex-stage::before,
  .vortex-svg,
  .artifact {
    display: none;
  }

  .orbit-card {
    position: relative;
    inset: auto;
    width: 100%;
    min-height: 86px;
    padding-left: 3.1rem;
  }

  .orbit-card::before {
    content: "";
    position: absolute;
    left: 1.05rem;
    top: 1.1rem;
    width: 1rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--heat);
    box-shadow: 0 0 0 8px rgba(223, 155, 89, .12);
  }

  .orbit-card:not(:last-of-type)::after {
    content: "";
    position: absolute;
    left: 1.52rem;
    top: 2.35rem;
    bottom: -1.05rem;
    width: 2px;
    background: linear-gradient(var(--heat), rgba(139, 162, 118, .5));
  }

  .lot-grid,
  .ledger-steps,
  .club-flow,
  .tier-grid,
  .cupping__panel {
    grid-template-columns: 1fr;
  }

  .lot-card {
    min-height: auto;
  }

  .cupping__panel {
    min-height: auto;
  }

  .cupping__panel > img {
    aspect-ratio: 4 / 3;
  }

  .packing-panel {
    aspect-ratio: 4 / 3;
  }
}

@media (max-width: 25rem) {
  .nav-links a,
  .site-footer nav a {
    font-size: .7rem;
    padding-inline: .5rem;
  }

  .proof-row span,
  .mode-chips span {
    max-width: 100%;
  }
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: .001ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .001ms !important;
  }

  .roast-curve,
  .profile-curve__base {
    stroke-dashoffset: 0;
  }

  .bean--green { offset-distance: 24%; }
  .bean--copper { offset-distance: 55%; }
  .bean--red { offset-distance: 82%; }
}
